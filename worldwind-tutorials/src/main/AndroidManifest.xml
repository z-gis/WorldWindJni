<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 在线瓦片底图需要网络 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.WorldWindTutorials">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 各功能演示页 -->
        <activity android:name=".BasicMapActivity" />
        <activity android:name=".Globe3DActivity" />
        <activity android:name=".VectorLayerActivity" />
        <activity android:name=".OverlayActivity" />
        <activity android:name=".LocationMarkerActivity" />
        <activity android:name=".SrsConvertActivity" />
    </application>

</manifest>
